name: Strata Northstar T1 CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  northstar-t1:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Build backend
        run: |
          cd northstar/T1-wire-determinism/backend-rs
          cargo build

      - name: Start backend
        run: |
          cd northstar/T1-wire-determinism/backend-rs
          cargo run &
          sleep 2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install frontend deps
        run: |
          cd northstar/T1-wire-determinism/frontend-js
          npm install

      - name: Run Northstar T1
        run: |
          cd northstar/T1-wire-determinism/frontend-js
          node t1.test.js
